#!/usr/bin/env sh

# Start Rails server with Solid Queue running inline via Puma plugin.
# For development with puma-dev, this script is not needed — puma-dev
# starts Puma automatically. Use this for manual server start.
exec bin/rails server "$@"
