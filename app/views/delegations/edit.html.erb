<% content_for(:title, "Edit Delegation — Tech Lead OS") %>
<div class="container" style="max-inline-size: 40rem; margin-block-start: var(--size-6)">
  <h1 class="font-bold text-2xl" style="margin-block-end: var(--size-6)">Edit Delegation</h1>
  <div class="card"><%= render "form", delegation: @delegation, developers: @developers %></div>
</div>
