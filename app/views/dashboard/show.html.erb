<% content_for(:title, "Dashboard — Tech Lead OS") %>

<h1 class="font-bold text-2xl">Daily Lead Briefing</h1>

<div class="card text-center" style="color: var(--color-text-subtle)">
  <i class="ph ph-squares-four" style="font-size: var(--text-4xl); display: block; margin-block-end: var(--size-4)"></i>
  <p>Dashboard coming soon.</p>
  <p class="text-sm" style="margin-block-start: var(--size-2)">
    Check <%= link_to "system status", status_path %> to verify the stack.
  </p>
</div>
