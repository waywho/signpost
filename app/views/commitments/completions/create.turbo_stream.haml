= turbo_stream.replace dom_id(@commitment) do
  .card.flex.items-center.gap.opacity-50
    %i.ph.ph-check-circle.text-positive
    %span.text-sm.line-through.text-subtle.flex-1= @commitment.text
    %span.text-xs.text-subtle Done
